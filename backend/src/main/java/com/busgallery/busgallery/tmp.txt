const hasImages = computed(() => (images.value?.length || 0) > 0);
const hasExif = computed(() => images.value?.[0]?.exif && Object.keys(images.value[0].exif).length);

const vehicle = computed(() => props.detail?.vehicle || null);
const variants = computed(() => props.detail?.variants || []);
const currentImageIndex = ref(0);
const currentVariantIndex = ref(0);

watch(
    () => props.detail,
    () => {
        currentImageIndex.value = 0;
        currentVariantIndex.value = 0;
    }
);
